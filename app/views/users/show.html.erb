<%= link_to 'Back', stories_path %>
<br>


<div class="container">

   <%= render 'shared/authorcard', user: @user %>

  <% @stories.each do |s| %>
    <%#= link_to 'Stories Setting', functionality_path(s.id) %>
    <br>
    <%= s.title %>
    <%= link_to 'Show', s %>
    <%= render "functionality", s: s %>
  <% end %>
  <br>
  <br>
    <%= link_to 'NEW STORY', new_story_path, class: "btn-gold" %>
</div>

<%#= link_to 'Edit', edit_story_path(@story) %>
<%#= link_to 'Delete', story_path(@story), method: :delete, data: {confirm: "you sure mec?"} %>
