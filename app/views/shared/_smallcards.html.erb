
  <div class="card-product">
     <%= cl_image_tag s.images.first.data %>
    <div class="card-product-infos">
      <h3> <%= link_to "#{s.title.truncate(40)}", story_path(s) %></h3>
      <p> <%= s.user.username %> </p>
    </div>
  </div>


